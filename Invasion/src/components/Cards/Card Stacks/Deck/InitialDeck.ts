import { Guard, GuardInformation, GuardAttributes } from "../../Battle Cards/Guard"
import { Card } from "../../Card"

export const INITIAL_DECK : Card[] = [
    new Guard(GuardInformation, GuardAttributes),
    new Guard(GuardInformation, GuardAttributes),
    new Guard(GuardInformation, GuardAttributes),
    new Guard(GuardInformation, GuardAttributes),
    new Guard(GuardInformation, GuardAttributes),
    new Guard(GuardInformation, GuardAttributes),
]